# Public frontend config only (safe to expose in browser bundles)
VITE_API_BASE_URL=http://13.218.173.57:3002

# Do not place secrets/tokens/passwords in VITE_ variables.
# Keep all sensitive keys on the backend/server-side secret store.
